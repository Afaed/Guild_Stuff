"""
Function Drills 10: Strings 2

This module will help you improve your understanding of functions and strings.
Each function below contains a test. Execute this script to see the results.

Instructions
------------

1. Read the description of each function.
2. Code your solution. All solutions are valid.
3. Pass all the tests!

To run the tests, simply execute this python script from the CLI.
Always solve 1 problem at a time.
"""


def find_and_remove(text: str, prefix: str) -> str:
    """
    Given a stringvariable size, find all words that start with the prefix
    and remove them.
    """
    # your code here


def get_letters_in_range(letters: str) -> str:
    """
    Given a string that contains 2 letters, return the sequence of the alphabet between them (inclusive).
    The alphabet contains all letters from a-z. 

    :param letters: str of length 3 contain 2 letter separated by a hyphen

    Example:
    >>> get_letters_in_range('r-y')
    'rstuvwxy'
    """
    # your code here


def is_palindrome(text: str) -> bool:
    """
    Given a string of variable length, determine if its a palindrome. A
    palindrome is a string that reads the same forwards and backwards.

    The string will always contain at least 1 letter and will not contain
    punctuation or numbers.

    Example:
    >>> is_palindrome('kayak')
    True
    >>> is_palindrome('deified')
    True
    >>> is_palindrome('newspaper')
    False
    >>> is_palindrome('Y')
    True
    """
    # your code here




